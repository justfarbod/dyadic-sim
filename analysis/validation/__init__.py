"""Validation analyses for the symptom-injection manipulation.

These scripts check whether the patient agent actually expresses the PHQ-9
symptom injected into its prior (construct validity of the manipulation), with
a lexicon panel as the primary instrument and an embedding measure as convergent
evidence. Run them from the repo root with ``PYTHONPATH=.``; see ``paths.py``
for the input/output layout.
"""